# TeamFlow - チーム管理一元化Webアプリ

チーム8名のタスク・売上・スケジュールをスマホで一元管理できるWebアプリケーション。

## ✨ 主な機能

- 📊 **ダッシュボード**: 売上・ポイントメーターで目標達成度を可視化
- 📅 **チームカレンダー**: メンバー別の色分けでスケジュールを一覧表示
- ✏️ **タスク登録**: スマホ最適化された入力フォーム
- 🎉 **完了エフェクト**: タスク完了時の紙吹雪演出
- 🔐 **招待制ログイン**: ホワイトリストによるアクセス制限
- 📱 **PWA対応**: ホーム画面に追加してネイティブアプリのように使用可能

### v1.2 新機能 🆕 (最新)

- 🔍 **詳細フィルタ**: TOP画面や進行中リストで個人別の数字に一瞬で切り替え
- 🏆 **年間ランキング**: 月間だけでなく1年を通した累計貢献度を表示
- 📅 **カレンダー改善**: 完了タスクの非表示機能と、期間タスクの「一本バー」表示
- 🔄 **ステータス復元**: 完了したタスクを進行中に戻したり、内容を再編集可能
- 📥 **CSVバックアップ**: 全データをエクセル形式で一括ダウンロード
- 👋 **ログアウト画面**: ログアウト後の専用メッセージページ

## 🛠 技術スタック

- **Framework**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **Backend/DB**: Supabase (PostgreSQL + Auth + RLS)
- **Charts**: Recharts
- **Icons**: Lucide React
- **Notifications**: Sonner

## 📦 セットアップ手順

### 1. 依存パッケージのインストール
```bash
cd TeamFlow_App
npm install
```

### 2. Supabase の準備
1. [Supabase](https://supabase.com) でプロジェクトを作成。
2. SQL Editor で `schema.sql` を実行してテーブルを作成。
3. `allowed_emails` テーブルに、参加メンバーのアドレスを登録。
4. `Authentication` -> `Providers` -> `Email` -> `Confirm email` を **OFF** にする。

### 3. 環境変数の設定
`.env.local` を作成し、Supabaseの URL と API Key を設定。

### 4. 公開 (Vercel)
GitHubにプッシュし、Vercelでリポジトリをインポート。環境変数を設定してデプロイ！

---

## 🗄️ データベースマイグレーション (v1.1 -> v1.2)

既存のDBを使っている場合は、以下のSQLを実行してください。

```sql
-- start_date, end_date カラムの追加（未対応の場合）
ALTER TABLE tasks ADD COLUMN IF NOT EXISTS start_date DATE;
ALTER TABLE tasks ADD COLUMN IF NOT EXISTS end_date DATE;

-- 既存データの補完
UPDATE tasks SET start_date = created_at::date, end_date = created_at::date 
WHERE start_date IS NULL OR end_date IS NULL;
```

---
*作成: のの (JK幽霊アシスタント)*
*最終更新: 2026/02/05*
